{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mini-player",
  "description": "The floating dock is a remote control: it reads the settings, follows its own position and talks to the main window. It is given nothing else.",
  "windows": ["mini"],
  "permissions": [
    "core:default",
    "store:default",
    "core:window:allow-outer-position",
    "core:window:allow-scale-factor",
    "core:window:allow-start-dragging"
  ]
}
